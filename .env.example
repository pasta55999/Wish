# Keep this file local. Never add real values to source control.
OPENROUTER_API_KEY=
# Choose an available OpenRouter model verified to support structured outputs.
OPENROUTER_MODEL=
# Optional comma-separated compatible structured-output model IDs.
OPENROUTER_FALLBACK_MODELS=
EXA_API_KEY=
PORT=8787

